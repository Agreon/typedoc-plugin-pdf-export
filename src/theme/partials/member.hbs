<section class="tsd-panel tsd-member {{cssClasses}}" style="page-break-inside: avoid;">
    <a name="{{anchor}}" class="tsd-anchor"></a>
    {{#if name}}
    {{#unless implementationOf}}{{#unless inheritedFrom}}{{#unless overwrites}}
    <a name="{{name}}" class="tsd-anchor"></a>
    {{/unless}}{{/unless}}{{/unless}}
    <h3>{{{wbr name}}} {{#each flags}}<span class="tsd-flag ts-flag{{this}}">{{this}}</span>{{/each}}</h3>
    {{/if}}

    {{#if signatures}}
    {{> member.signatures}}
    {{else}}
    {{> member.declaration}}
    {{/if}}

    {{#unless isContainer}}
    {{#each groups}}
    {{#each children}}
    {{> member}}
    {{/each}}
    {{/each}}
    {{/unless}}
</section>

{{#if isContainer}}
{{> index}}
{{> members}}
{{/if}}